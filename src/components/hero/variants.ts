// import { HeroVariant } from "./types";

import { HeroVariant } from "@/components/hero/types";

export const heroVariantMap: Record<HeroVariant, string> = {
  center: "hero-center",
  left: "hero-left",
  right: "hero-right",
  split: "hero-split",
  video: "hero-video",
  minimal: "hero-minimal",
  form: "hero-form",
  carousel: "hero-carousel",
  stats: "hero-stats",
  illustration: "hero-illustration",
  shapeDivider: "hero-shapeDivider",
};
